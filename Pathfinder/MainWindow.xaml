<Window x:Class="Pathfinder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Pathfinder.ViewModels"
		xmlns:v="clr-namespace:Pathfinder.Views"
		xmlns:pathfinder="clr-namespace:Pathfinder"
		Title="MainWindow" Height="600" Width="800">
	<Window.Resources>
		<DataTemplate DataType="{x:Type vm:ListViewModel}">
			<v:ListView />
		</DataTemplate>
		<DataTemplate DataType="{x:Type vm:TreeViewModel}">
			<v:TreeView />
		</DataTemplate>
	</Window.Resources>
	<Window.DataContext>
		<vm:ShellViewModel/>
	</Window.DataContext>
	<Grid>
		<Menu HorizontalAlignment="Stretch" Height="20" VerticalAlignment="Top">
			<MenuItem Header="View" >
				<MenuItem Header="Tree" HorizontalAlignment="Left" Width="145" Command="{Binding SwitchToTreeViewCommand}"/>
				<MenuItem Header="List" HorizontalAlignment="Left" Width="145" Command="{Binding SwitchToListViewCommand}"/>
			</MenuItem>
		</Menu>
		<ContentControl Content="{Binding Contenth}" VerticalAlignment="Stretch" Margin="0 5 0 0"/>
	</Grid>
</Window>
